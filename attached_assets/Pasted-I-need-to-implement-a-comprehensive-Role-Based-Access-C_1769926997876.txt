I need to implement a comprehensive Role-Based Access Control (RBAC) system for my platform.

Please refactor the database, backend middleware, and frontend UI to support the following 4 roles: 'Student', 'Admin', 'Super Admin', and 'Accountant'.

Here are the specific requirements for each role and the logic implementation:

1. **Database Schema Update:**
   - Update the `User` table to include a `role` field (Enum or String: 'student', 'admin', 'super_admin', 'accountant').
   - Default role for new sign-ups should be 'student'.

2. **Role Permissions & Logic:**

   - **Student:**
     - Can only view/buy courses and manage their own profile.
   
   - **Accountant (New Role):**
     - **Access:** Read-only access to a new "Financial Dashboard" (Total sales, revenue, transaction history).
     - **Actions:** Can create/edit/delete Discount Coupons.
     - **Restriction:** CANNOT view course content, edit users, or delete courses.

   - **Admin:**
     - Can manage users (ban/unban) and manage content.
     - **CRITICAL FEATURE:** Admins must be able to create/delete courses *on behalf of* other instructors. Add a field in the "Create Course" form to select the "Course Author/Instructor" from existing users.

   - **Super Admin (The Owner):**
     - Has full unrestricted access to EVERYTHING (God Mode).
     - **CRITICAL FEATURE:** Add a "User Management" table where the Super Admin can change the `role` of ANY user (e.g., promote a Student to Admin or Accountant) at any time.
     - Can edit system settings, payment keys, and view all financial data.

3. **Implementation Details:**
   - **Middleware:** Create a middleware function (e.g., `checkRole(['admin', 'super_admin'])`) to protect API routes.
   - **Frontend:** Update the Dashboard sidebar/navigation. conditionally render links based on the logged-in user's role.
     - *Example:* Only show "Financials" tab to Accountant and Super Admin.
     - *Example:* Only show "User Management" to Super Admin.

Please proceed with updating the database schema, creating the necessary API endpoints, and updating the UI components.