I need to implement a robust Role-Based Access Control (RBAC) system for my educational platform.

Current Context:
- The app has 3 existing roles: Student, Admin, Super Admin.
- I need to add a 4th role: "Accountant".

Please perform the following updates to the Database, Middleware, and Routes:

1. Database Schema Update:
   - Update the User model/table to include a `role` field.
   - Allowed values (Enums): 'student', 'admin', 'super_admin', 'accountant'.
   - Default value: 'student'.

2. Create Authorization Middleware:
   - Create a middleware function named `verifyRole(allowedRoles)`.
   - IMPORTANT: If the user's role is 'super_admin', they should bypass ALL checks and have access to everything (God Mode), regardless of the `allowedRoles` array passed to the middleware.

3. Specific Role Permissions & Logic:

   A. Super Admin (The Owner):
      - Has full CRUD access to every resource.
      - Create a specific endpoint: `PUT /api/admin/users/:id/role` that allows the Super Admin (and ONLY the Super Admin) to change the role of any user to any other role.

   B. Admin:
      - Can Create and Delete courses.
      - Logic Update for Course Creation: When an Admin creates a course, allow them to specify an `instructorId` in the body to assign the course to a specific teacher. If a normal instructor creates it, it defaults to their own ID.

   C. Accountant:
      - Can ONLY access financial routes (e.g., GET /api/finance/transactions, GET /api/finance/invoices).
      - Cannot edit or delete any course content.

   D. Student:
      - Read-only access to courses they purchased.
      - Can submit work/quizzes.

4. Implementation Steps:
   - Update the User schema.
   - Write the `verifyRole` middleware.
   - Update the "Create Course" route to handle the `instructorId` assignment by Admins.
   - Create the "Change User Role" route protected for Super Admin only.

Refactor the existing routes to use this new middleware.